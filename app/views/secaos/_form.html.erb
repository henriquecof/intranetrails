<%= form_with(model: secao, local: true) do |form| %>
  <% if secao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secao.errors.count, "error") %> prohibited this secao from being saved:</h2>

      <ul>
        <% secao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
